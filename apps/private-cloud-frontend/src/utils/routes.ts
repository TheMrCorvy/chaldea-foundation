export enum WebRoutes {
    HOME = "/",
    LOGIN = "/login/",
    REGISTER = "/register/",
    SEARCH = "/search/",
    DIRECTORY = "/directory/",
    EPISODE = "/episode/",
    PENDING_USER_ACTIVATION = "/pending-activation/",
}

export enum ApiRoutes {
    MOCK_SESSION = "/api/set-mock-session/",
    STREAM_MEDIA_SECURE = "/api/stream-media-secure",
}

export enum NasApiRoutes {
    STREAM_MEDIA = "/api/stream-media/",
}
